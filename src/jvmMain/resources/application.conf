ktor {
development = true
    deployment {
        watch = [ classes ]
    }
    deployment {
        host = ${IP_ADDR}
        port = 8080
    }
    application {
        modules = [ ServerKt.module ]
    }
}
